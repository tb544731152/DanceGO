<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.realfans.dancego">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.beautifulsoup.dancego.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.beautifulsoup.dancego.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.beautifulsoup.dancego.activity.MainActivity" />
        <activity
            android:name="com.beautifulsoup.dancego.activity.GuideActivity"
            android:label="@string/title_activity_guide"
            android:theme="@style/AppTheme" />
        <activity android:name="com.beautifulsoup.dancego.activity.LoginActivity"/>
        <activity android:name="com.beautifulsoup.dancego.activity.RegisterActivity"/>
        <activity android:name="com.beautifulsoup.dancego.activity.VideoPlayerActivity"/>
    </application>

</manifest>